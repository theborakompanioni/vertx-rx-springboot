// Generated by delombok at Sat Jan 20 12:35:44 CET 2018
package io.vertx.example;

import com.google.common.collect.ImmutableList;
import io.vertx.core.Verticle;
import io.vertx.rxjava.core.RxHelper;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import rx.Observable;
import rx.observables.BlockingObservable;
import javax.annotation.PostConstruct;
import java.util.List;
import static java.util.Objects.requireNonNull;

@SpringBootApplication
public class Application {
    @java.lang.SuppressWarnings("all")
    @javax.annotation.Generated("lombok")
    private static final org.slf4j.Logger log = org.slf4j.LoggerFactory.getLogger(Application.class);

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    private io.vertx.rxjava.core.Vertx vertx;
    private List<Verticle> verticles;

    @Autowired
    public Application(io.vertx.rxjava.core.Vertx vertx, List<Verticle> verticles) {
        this.vertx = requireNonNull(vertx);
        this.verticles = ImmutableList.copyOf(verticles);
    }

    @PostConstruct
    public void deployVerticle() {
        Observable.from(verticles).map(verticle -> RxHelper.deployVerticle(vertx, verticle)).map(Observable::toBlocking).map(BlockingObservable::single).forEach(response -> {
            log.debug("deployed verticle {}", response);
        });
    }
}
